# Copy this file to os/secrets.toml (git-ignored) and fill it in.
# The values are baked into the firmware as defaults. They are used until
# settings saved on the device replace them.
wifi_ssid = "MyNetwork"
wifi_password = "hunter2"
frame_server = "http://192.168.1.9:8000"
frame_name = "arpan"
